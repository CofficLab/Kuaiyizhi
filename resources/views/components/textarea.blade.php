<div>
    <textarea id="{{ $name }}" wire:model.lazy="{{ $name }}" placeholder="{{ $placeholder }}"
        name="message" class="text-center flex-1 block rounded-3xl
        sm:text-sm border-gray-300 h-64 w-96 shadow-lg 
        dark:bg-gray-800 dark:text-blue-100 dark:border-gray-600"></textarea>
</div>
