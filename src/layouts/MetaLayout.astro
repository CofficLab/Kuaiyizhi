---
import AppLayout from './AppLayout.astro';
import { getMetaSidebarItem } from '../database/meta';
import { logger } from '../utils/logger';

interface Props {
  title: string;
  description: string;
  headings?: Array<{ depth: number; slug: string; text: string }>;
  lang: string;
}

const debug = false;
const { title, description, headings = [], lang } = Astro.props;
const sidebarItems = await getMetaSidebarItem(lang);

if (debug) {
  logger.array(`meta sidebarItems`, sidebarItems.toStarlightFormat());
}
---

<AppLayout
  title={title}
  description={description}
  template="doc"
  frontmatter={{
    title: title,
    description: description,
    template: 'doc',
  }}
  hasSidebar={true}
  sidebar={sidebarItems.toStarlightFormat()}
  headings={headings}
  hidePageTitle={true}>
  <slot />
</AppLayout>
