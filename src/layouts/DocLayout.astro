---
import type { SidebarItem } from '@/models/SidebarItem';
import AppLayout from './AppLayout.astro';

interface Props {
  title: string;
  description: string;
  sidebarItem: SidebarItem;
  hasSidebar: boolean;
  headings?: Array<{ depth: number; slug: string; text: string }>;
}

const {
  title,
  description,
  sidebarItem,
  headings = [],
  hasSidebar,
} = Astro.props;
---

<AppLayout
  title={title}
  description={description}
  template="doc"
  frontmatter={{
    title: title,
    description: description,
    template: 'doc',
  }}
  sidebar={sidebarItem}
  headings={headings}
  hidePageTitle={false}>
  <div
    class="markdown-content prose prose-slate dark:prose-invert max-w-none card border-2 border-gray-200 dark:border-gray-800 p-4">
    <slot />
  </div>
</AppLayout>
