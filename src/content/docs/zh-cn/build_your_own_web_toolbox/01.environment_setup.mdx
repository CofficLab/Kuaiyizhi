---
title: 环境搭建
description: 配置在线工具箱项目的开发环境
---

## 开发环境准备

在开始开发我们的在线工具箱项目之前，我们需要先配置好必要的开发环境。本文将指导你完成所有环境配置步骤。

### 安装 Node.js

1. 访问 [Node.js 官网](https://nodejs.org/)
2. 下载并安装 LTS（长期支持）版本
3. 安装完成后，打开终端验证安装：

```bash
# 检查 Node.js 版本
node --version

# 检查 npm 版本
npm --version
```

确保 Node.js 版本 >= 16.0.0。

### 安装 VS Code

1. 访问 [Visual Studio Code 官网](https://code.visualstudio.com/)
2. 下载并安装 VS Code
3. 安装以下推荐的扩展：
   - Vue Language Features (Volar) - Vue 3 支持
   - TypeScript Vue Plugin (Volar)
   - ESLint - 代码质量检查
   - Prettier - 代码格式化
   - Tailwind CSS IntelliSense - TailwindCSS 智能提示

### 安装 Git

1. 访问 [Git 官网](https://git-scm.com/)
2. 下载并安装 Git
3. 配置 Git 全局设置：

```bash
# 设置用户名和邮箱
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"

# 验证配置
git config --list
```

### 创建 GitHub 账号

1. 访问 [GitHub](https://github.com/)
2. 注册新账号或登录已有账号
3. 配置 SSH 密钥（推荐）：

```bash
# 生成 SSH 密钥
ssh-keygen -t ed25519 -C "你的邮箱"

# 复制公钥内容
cat ~/.ssh/id_ed25519.pub
```

4. 将公钥添加到 GitHub 账号设置中

## 项目初始化

### 创建 Nuxt.js 项目

1. 使用 Nuxt.js 脚手架创建项目：

```bash
# 创建新项目
npx nuxi init web-toolbox

# 进入项目目录
cd web-toolbox

# 安装依赖
npm install
```

2. 添加必要的依赖：

```bash
# 安装 TailwindCSS 和 DaisyUI
npm install -D tailwindcss postcss autoprefixer daisyui

# 初始化 TailwindCSS 配置
npx tailwindcss init
```

### 配置项目

1. 创建 `.gitignore` 文件：

```plaintext
# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.env
.env.*
!.env.example

# Editor
.vscode/*
!.vscode/extensions.json
.idea
```

2. 配置 TailwindCSS（`tailwind.config.js`）：

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./components/**/*.{js,vue,ts}",
    "./layouts/**/*.vue",
    "./pages/**/*.vue",
    "./plugins/**/*.{js,ts}",
    "./app.vue",
  ],
  theme: {
    extend: {},
  },
  plugins: [require("daisyui")],
  daisyui: {
    themes: true,
  },
}
```

3. 初始化 Git 仓库：

```bash
# 初始化仓库
git init

# 添加文件
git add .

# 创建首次提交
git commit -m "初始化项目"
```

### 启动开发服务器

```bash
# 启动开发服务器
npm run dev
```

访问 `http://localhost:3000` 查看项目。

## 下一步

环境配置完成后，我们将开始进行项目的基础开发，包括：

- 设计项目结构
- 配置路由系统
- 开发基础组件

请确保所有工具都已正确安装和配置，这将为后续的开发工作打下良好的基础。