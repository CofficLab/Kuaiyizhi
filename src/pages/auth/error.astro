---
import AuthErrorView from '@/views/AuthErrorView.astro';

// 获取用户语言设置，这里简单判断，你可以根据实际需求修改
const userLang = Astro.request.headers.get('accept-language')?.includes('zh')
  ? 'zh-cn'
  : 'en';
---

<AuthErrorView lang={userLang} />
