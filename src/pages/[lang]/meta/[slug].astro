---
import MetaView from '@/views/MetaView.astro';
import metaDB from '@/database/MetaDB';

export const prerender = true;
export async function getStaticPaths() {
  return metaDB.getStaticPaths();
}

const { lang, slug } = Astro.params;
const id = `${lang}/${slug}`;
---

<MetaView id={id} />
