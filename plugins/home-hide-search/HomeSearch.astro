---
import type { Props } from '@astrojs/starlight/props';
import Default from '@astrojs/starlight/components/Search.astro';

const props = Astro.props as Props;
const isHomepage = props.id === '';
---

{!isHomepage && <Default {...props} />}
